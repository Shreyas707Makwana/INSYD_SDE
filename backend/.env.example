# Backend environment variables
# All MUST_REPLACE before running in production.

PORT=4000
SUPABASE_URL=MUST_REPLACE
SUPABASE_SERVICE_ROLE_KEY=MUST_REPLACE
# Optional CORS origin
FRONTEND_ORIGIN=http://localhost:3000
