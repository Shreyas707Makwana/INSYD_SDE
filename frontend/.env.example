# Frontend envs (public)
# Replace with your Supabase project values.
NEXT_PUBLIC_SUPABASE_URL=MUST_REPLACE
NEXT_PUBLIC_SUPABASE_ANON_KEY=MUST_REPLACE
# Backend base URL used by client-side fetches
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
